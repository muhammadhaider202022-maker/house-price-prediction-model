[project]
name = "house-price-prediction-model"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "pandas>=1.5.0",
    "scikit-learn>=1.2.0",
    "joblib>=1.2.0"
]

[tool.vercel]
entrypoint = "app:app"
